name: armv7_gcc_uclibc Build

on:
  push:
    branches:
      - armv7_gcc_uclibc
jobs:
  armv7_gcc_uclibc-build:
    name: armv7_gcc_uclibc Build
    runs-on: ubuntu-24.04
    steps:
      - name: Build Pack armv7_gcc_uclibc
        run: |
          sudo apt update
          sudo apt install 7zip wget build-essential gcc make libncurses5-dev qemu-user -y
          git clone --depth=1 https://github.com/gxiGpf/build -b armv7_gcc_uclibc
          wget https://buildroot.org/downloads/buildroot-2024.02.9.tar.gz
          tar -zxvf buildroot-2024.02.9.tar.gz
          cp build/.config buildroot-2024.02.9/
          cd buildroot-2024.02.9
          make sdk V=0
          pwd
      - name: Pack toolchain
        run: |
          7z a /home/runner/work/armv7_gcc_uclibc_linux4.9.37.7z /home/runner/work/build/build/buildroot-2024.02.9/output/images
      - name: Update buildroot Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: buildroot-build
          files: |
            /home/runner/work/armv7_gcc_uclibc_linux4.9.37.7z

