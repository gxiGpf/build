name: llvm Build

on:
  workflow_dispatch:
  push:
    branches:
      - zig
jobs:
  # llvm-build:
  #   name: llvm Build
  #   runs-on: windows-2019
  #   steps:
  #     - uses: ilammy/msvc-dev-cmd@v1
  #       with:
  #         arch: amd64
  #     - name: Prepare
  #       run: |
  #         cd C:\ ; git clone --depth 1 --branch release/20.x https://github.com/llvm/llvm-project llvm
  #         cd llvm ; git checkout release/20.x ; mkdir build-release ; cd build-release
  #         cmake ..\llvm -DCMAKE_INSTALL_PREFIX="C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt" -DCMAKE_BUILD_TYPE=Release -DLLVM_ENABLE_PROJECTS="lld;clang" -DLLVM_ENABLE_LIBXML2=OFF -DLLVM_ENABLE_TERMINFO=OFF -DLLVM_ENABLE_LIBEDIT=OFF -DLLVM_ENABLE_ASSERTIONS=ON -DLLVM_PARALLEL_LINK_JOBS=1 -GNinja -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded
  #         ninja install
  #     - name: Pack llvm
  #       run: |
  #         7z a C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt.7z C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: llvm
  #         path: C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt.7z
  #     - name: Update llvm Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         tag_name: zig-build
  #         files: |
  #           C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt.7z

  # zig-build:
  #   name: zig Build
  #   runs-on: windows-2019
  #   steps:
  #     - uses: ilammy/msvc-dev-cmd@v1
  #       with:
  #         arch: amd64
  #     - name: Build
  #       run: |
  #         choco install wget
  #         cd C:\ ; wget -q https://github.com/gxiGpf/build/releases/download/zig-build/llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt.7z
  #         7z x C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt.7z -oC:\ ; rm C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt.7z
  #         vcpkg install zlib:x64-windows-static zstd:x64-windows-static
  #         cd C:\ ; git clone --depth 1 https://github.com/ziglang/zig 
  #         wget -q https://raw.githubusercontent.com/gxiGpf/build/refs/heads/zig/0.patch ; cd zig
  #         git apply ..\0.patch
  #         mkdir build ; cd build
  #         cmake .. -GNinja -DCMAKE_PREFIX_PATH="C:\llvm+clang+lld-20.x-x86_64-windows-msvc-release-mt;C:\vcpkg\installed\x64-windows-static\lib" -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=C:\zig-release -DZIG_STATIC=ON -DZIG_USE_LLVM_CONFIG=OFF -DZIG_TARGET_TRIPLE="x86_64-windows-msvc"
  #         ninja install
  #     - name: Pack
  #       run: |
  #         7z a C:\zig-custom-master.7z C:\zig\build\stage3
  #     - uses: actions/upload-artifact@v4
  #       with:
  #         name: zig
  #         path: C:\zig-custom-master.7z
  #     - name: Update Zig Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         tag_name: zig-build
  #         files: |
  #           C:\zig-custom-master.7z

  x86_64-windows-gnu-build:
    name: Zig x86_64-windows-gnu Build
    runs-on: windows-2019
    steps:
      - uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: amd64
      - name: Build
        run: |
          choco install wget
          cd C:\ ; wget -q https://ziglang.org/deps/zig+llvm+lld+clang-x86_64-windows-gnu-0.15.0-dev.233+7c85dc460.zip
          7z x zig+llvm+lld+clang-x86_64-windows-gnu-0.15.0-dev.233+7c85dc460.zip -oC:\ ; rm zig+llvm+lld+clang-x86_64-windows-gnu-0.15.0-dev.233+7c85dc460.zip
          mv zig+llvm+lld+clang-x86_64-windows-gnu-0.15.0-dev.233+7c85dc460.zip zig-bootstrap-release
          cd C:\ ; git clone --depth 1 https://github.com/ziglang/zig 
          wget -q https://raw.githubusercontent.com/gxiGpf/build/refs/heads/zig/0.patch ; cd zig
          git apply ..\0.patch
          mkdir build ; cd build
          cmake .. -GNinja -DCMAKE_PREFIX_PATH="C:\zig-bootstrap-release" -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=C:\zig-release -DZIG_STATIC=ON -DZIG_NO_LIB=ON -DZIG_AR_WORKAROUND=ON \
            -DZIG_TARGET_TRIPLE="x86_64-windows-gnu" -DZIG_TARGET_MCPU="skylake" \
            -DCMAKE_C_COMPILER="C:\zig-bootstrap-release\bin\zig.exe;cc;-target;x86_64-windows-gnu;-mcpu=skylake" \
            -DCMAKE_CXX_COMPILER="C:\zig-bootstrap-release\bin\zig.exe;c++;-target;x86_64-windows-gnu;-mcpu=skylake" \
            -DCMAKE_AR="C:\zig-bootstrap-release\bin\zig.exe"
          ninja install
      - name: Pack
        run: |
          7z a C:\zig-custom-x86_64-windows-gnu-master-release.7z C:\zig-release
      - name: Update Zig Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: zig-build
          files: |
            C:\zig-custom-x86_64-windows-gnu-master-release.7z

  # gen-compilation-database-master-patch-build:
  #   name: zig Build
  #   runs-on: windows-2022
  #   steps:
  #     - name: Prepare
  #       env:
  #         GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       run: |
  #         choco install wget
  #         cd C:\ ; git clone --depth 1 https://github.com/ziglang/zig ; cd zig
  #         wget -q https://github.com/gxiGpf/build/releases/download/zig-build/Support-generating-JSON-compilation-database.patch
  #         git apply Support-generating-JSON-compilation-database.patch
  #         7z a C:\Support-generating-JSON-compilation-database-files.7z C:\zig\lib\std\Build.zig C:\zig\lib\std\Build\Step\Compile.zig
  #     - name: Update zig Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         tag_name: zig-build
  #         files: |
  #           C:\Support-generating-JSON-compilation-database-files.7z